---
# useradd handlers

- name: restart sshd
  ansible.builtin.systemd_service:
    state: "restarted"
    enabled: true
    name: "sshd"

- name: reload sshd
  ansible.builtin.systemd_service:
    state: "reloaded"
    enabled: true
    name: "sshd"
